{
  "extends": "astro/tsconfigs/strict",
  "compilerOptions": {
    "strictNullChecks": true,
    "types": ["astro/client"]
  },
  "include": [".astro/types.d.ts", "**/*"],
  "comment:exclude": "Test files use bun:test; scripts/ is a Bun build script with its own runtime. Neither is type-checked by astro check.",
  "exclude": ["dist", "scripts", "src/**/*.test.ts"]
}
